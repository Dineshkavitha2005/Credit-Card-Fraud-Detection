services:
  # ============================================================================
  # Credit Card Fraud Detection Web & ML Service
  # ============================================================================
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fraud_detection_app
    restart: unless-stopped
    ports:
      - "${PORT:-5000}:5000"
    env_file:
      - .env
    environment:
      - FLASK_ENV=production
      - PORT=5000
      - DATABASE_URL=${DATABASE_URL:-postgresql://${POSTGRES_USER:-fraud_user}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-fraud_db}}
      - POSTGRES_USER=${POSTGRES_USER:-fraud_user}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB:-fraud_db}
      - SECRET_KEY=${SECRET_KEY}
      - CARD_ENCRYPTION_KEY=${CARD_ENCRYPTION_KEY}
      - WORKERS=${WORKERS:-4}
      - THREADS=${THREADS:-2}
      - TIMEOUT=${TIMEOUT:-120}
      - LOG_LEVEL=${LOG_LEVEL:-info}
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID:-}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET:-}
      - GOOGLE_REDIRECT_URI=${GOOGLE_REDIRECT_URI:-}
      - GOOGLE_ALLOWED_DOMAIN=${GOOGLE_ALLOWED_DOMAIN:-}
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      # Persist SQLite database (if used in development)
      - app_data:/app/data
      # Persist generated PDF & CSV fraud reports
      - reports_data:/app/instance/reports
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:5000/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 25s
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 2G
        reservations:
          cpus: '0.5'
          memory: 512M

  # ============================================================================
  # Primary Production PostgreSQL Database Service (Default)
  # ============================================================================
  postgres:
    image: postgres:15-alpine
    container_name: fraud_detection_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-fraud_db}
      POSTGRES_USER: ${POSTGRES_USER:-fraud_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - db_data:/var/lib/postgresql/data
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-fraud_user} -d ${POSTGRES_DB:-fraud_db}"]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 10s

# ==============================================================================
# Persistent Named Volumes
# ==============================================================================
volumes:
  app_data:
    name: fraud_detection_app_data
    driver: local
  reports_data:
    name: fraud_detection_reports_data
    driver: local
  db_data:
    name: fraud_detection_db_data
    driver: local
